// collect the user data
// we verify if the user is not an existing user
// hashing of password
// save the user to the database
// send mail to a user

function functionName(parameters) {
  // function body (this is where you write the logic that will perform
  //   the fuction)
}
// Parameters are the variables needed for the function to complete its operation

function createUser(firstName, lastName, email, password) {
  // collect the user data
  verifyUser();
  hashPassword();
  saveUser();
}

function verifyUser() {
  //this method verifies a user
}

function hashPassword() {
  // this function hashes a password
}

function saveUser() {
  // this function saves a user to the database
}
