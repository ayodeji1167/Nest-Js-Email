// write a function that takes two parameters and prints the
// addition of those parameters

function addition(num1, num2) {
  console.log(num1 + num2);
}

/**
 * You use a function by invoking it
 */

addition(12, 10);
